global class Application implements Comparable {

    public String name;
    public Integer amount;

    public Application(String name Integer amount) {
        name = n;
        amount = a;
    }

    global Integer compareTo(Object compareTo) {
        Application compareToApp = (Application)compareTo;
        Integer returnValue = 0;
        if (amount > compareToApp.amount) {
            returnValue = 1;
        } else if (amount < compareToApp.amount) {
            returnValue = -1;
        }

        return returnValue;
    }
}